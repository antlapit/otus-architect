apiVersion: v1
kind: Service
metadata:
  name: {{ include "otus-architect.fullname" . }}
  labels:
    {{- include "otus-architect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: web
  selector:
    {{- include "otus-architect.selectorLabels" . | nindent 4 }}
