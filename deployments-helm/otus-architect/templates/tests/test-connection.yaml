apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otus-architect.fullname" . }}-test-connection"
  labels:
    {{- include "otus-architect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otus-architect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
