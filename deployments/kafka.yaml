service:
  type: NodePort

externalAccess:
  enabled: true
  service:
    type: NodePort
    domain: "arch.homework"
    nodePorts:
      - 32000

persistence:
  size: 1Gi

logPersistence:
  size: 1Gi

rbac:
  create: true
